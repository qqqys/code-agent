# 权限与沙箱矩阵

[返回文档目录](./README.md) · [网页矩阵](https://qqqys.github.io/code-agent/#security) · [详情目录](./capabilities/security/)

> 核对日期：2026-08-08

| 能力 | Claude Code | Codex | Qwen Code | Kimi Code | Qoder CLI |
| --- | --- | --- | --- | --- | --- |
| [交互审批](./capabilities/security/security-approval.md) | 支持 | 支持 | 支持 | 支持 | 支持 |
| [只读或计划模式](./capabilities/security/security-plan.md) | Plan Mode | `/plan` 与 `read-only` | `plan` approval mode | `/plan` | `/plan` 独立工作状态 |
| [自动接受编辑](./capabilities/security/security-auto-edit.md) | `acceptEdits` | `workspace-write` 配合 `on-request` | `auto-edit` | 无仅编辑模式；`/auto` 范围更广 | `accept_edits` · `acceptEdits` |
| [跳过审批](./capabilities/security/security-bypass.md) | `bypassPermissions` | `approval_policy = "never"`；全放开需 `danger-full-access` | `yolo` | `/yolo` | `bypass_permissions` · `yolo` |
| [文件系统隔离](./capabilities/security/security-filesystem.md) | Bash OS 沙箱 + 文件权限规则 | `read-only` · `workspace-write` · `danger-full-access` | Seatbelt 或容器 Sandbox；默认关闭 | 文件工具权限；OS 沙箱未确认 | 路径权限规则；SDK 条件 Sandbox |
| [网络隔离](./capabilities/security/security-network.md) | Sandbox 域名代理与 Allow/Deny；`strictAllowlist` 直接拒绝未列主机 | `workspace-write` 默认断网；可单独启用与限域 | Seatbelt Profile 与代理；依配置 | 网络工具权限；OS 网络隔离未确认 | Web 工具规则；SDK 条件网络 Sandbox |
| [凭据保护](./capabilities/security/security-credentials.md) | `sandbox.credentials.files` · `envVars` `deny`/`mask` · 出站代理注入 | `shell_environment_policy` 过滤子进程环境变量；无文件打码 | 官方沙箱文档未列凭据保护；容器挂载 `~/.qwen` | 未确认 OS 沙箱；文档未列凭据保护字段 | 本地 CLI/SDK 文档未列同类凭据保护 |
| [项目目录信任](./capabilities/security/security-trust.md) | Workspace Trust | Project Trust；未信任时跳过项目 `.codex/` | `/trust`；功能默认关闭 | 条件：v2 引擎启动信任提示；项目 MCP 门禁 | Trust Directories；未信任时回退 `default` |
| [非交互审批](./capabilities/security/security-noninteractive.md) | 未预授权操作中止；`dontAsk` 拒绝 | 无法展示的新审批返回错误 | 无法确认的工具调用拒绝；可预设 mode/rules | `-p` 固定使用 Auto；静态 Deny 仍生效 | `ask` 自动变为 `deny` |

## 阅读边界

审批策略决定工具是否需要停下来确认；沙箱决定命令和子进程实际能访问哪些文件、网络与系统资源。详情页会分别记录这两层，工具规则不会被当作 OS 沙箱。

## 详情字段

每个能力页分别记录五家的入口与切换、默认状态、具体行为、规则能力、隔离边界、保存与作用域、非交互行为、条件和官方来源。
