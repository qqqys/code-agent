# 权限与沙箱矩阵

[返回文档目录](./README.md) · [网页矩阵](https://qqqys.github.io/code-agent/#security)

| 能力 | Claude Code | Codex | Qwen Code | Kimi Code | Qoder CLI |
| --- | --- | --- | --- | --- | --- |
| 交互审批 | 支持 | 支持 | 支持 | 支持 | 支持 |
| 只读或计划模式 | Plan Mode | `/plan` 与只读沙箱组合 | `plan` approval mode | `/plan` | Agent `plan` permission mode |
| 自动接受编辑 | `acceptEdits` | `workspace-write` 配合审批策略 | `auto-edit` | `/auto` | `acceptEdits` |
| 跳过审批 | `bypassPermissions` | `danger-full-access` 或审批配置 | `yolo` | `/yolo` | `bypassPermissions` |
| 默认审批命令 | `/permissions` | `/permissions` | `/approval-mode`、`/permissions` | `/permission` | `/config` |
| 文件系统隔离 | 沙箱模式与权限规则 | `read-only`、`workspace-write`、`danger-full-access` | Sandbox 与工具权限；实现依运行配置 | 权限系统控制工具操作 | 权限模式；系统级隔离细节未确认 |
| 网络隔离 | 沙箱与策略配置 | `workspace-write` 可单独控制网络 | 依 Sandbox 和运行配置 | 权限系统；系统级网络隔离未确认 | 未确认 |
| 额外只读目录 | `/add-dir` 与权限规则 | `/sandbox-add-read-dir`，Windows 条件命令 | `/directory`、`/dir` | `/add-dir` | 未确认 |
| 项目目录信任 | 项目级信任流程 | 项目与沙箱配置 | `/trust` | 未确认 | 未确认 |
| 工具规则 | Allow/Deny 规则 | 审批策略、沙箱、工具配置 | `tools`、`excludeTools` 与权限规则 | 工具权限规则 | 工具与 permissionMode |
| 命令规则 | 支持 Bash 模式规则 | 支持命令审批策略 | 支持 Shell 工具规则 | 支持工具级权限 | 支持工具级权限 |
| MCP 工具审批 | 支持 | 支持 | 支持 | 支持 | 支持 |
| 企业集中策略 | Managed settings | Managed requirements/config | 管理配置依部署方式 | 未确认 | 产品账号与组织策略；细节未确认 |
| Subagent 权限 | 默认继承，可单独设 `permissionMode` | 继承会话，可设 `sandbox_mode` | `approvalMode`；父会话宽松模式优先 | 继承主会话 | `permissionMode`；省略时继承 |
| 非交互模式审批 | 由预设策略决定 | 无法展示的审批返回错误 | 由 approval mode 决定 | 由启动权限决定 | 由 permission mode 决定 |
| Windows 专用沙箱命令 | 未单列 | `/setup-default-sandbox`、`/sandbox-add-read-dir` | 未单列 | 未单列 | 未单列 |

## 说明

“会询问用户”和“操作系统级隔离”是两件事。审批策略决定是否需要确认；沙箱决定进程实际能访问哪些文件、网络和系统资源。

## 来源

- [Claude Code Permissions](https://code.claude.com/docs/en/permissions)
- [Codex Security](https://developers.openai.com/codex/security)
- [Qwen Code Configuration](https://github.com/QwenLM/qwen-code/tree/main/docs/users)
- [Kimi Code Permissions](https://github.com/MoonshotAI/kimi-code/tree/main/docs/zh)
- [Qoder CLI Permissions](https://docs.qoder.com/en/cli)
