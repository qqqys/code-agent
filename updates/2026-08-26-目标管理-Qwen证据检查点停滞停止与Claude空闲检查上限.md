# 目标管理字段：Qwen Code 证据检查点连续停滞自动停止，Claude Code v2.1.246 空闲检查上限

Qwen Code 于 2026-08-25 经 PR #9975（合并提交 `463809cbb39315b11dd9e410a2bcf5d52107418c`）为规范 Goal 运行时新增证据检查点停滞熔断：#9835 把截断的证据窗口改为压缩而不是停止 Goal 后，压缩无缓解的路径上不再有终止器，证据增速超过目录的 Goal 会每回合付一次检查点校验调用、每回合丢证据且永不收敛。新逻辑把“检查点在其压缩的窗口已截断时带回满额 32 条 claims（`GOAL_CHECKPOINT_CLAIM_LIMIT`）”或“窗口截断时校验器输出无法折叠成 claims（抛 `InvalidGoalCheckpointError`）”记为一次停滞；连续 3 次停滞（`GOAL_CHECKPOINT_STALL_LIMIT`）后 Goal 以 `usage_limited` 落定（`limitKind` 沿用 `evidence_catalog`，activity 置为 idle），固定原因说明自动重试无法恢复、建议 edit 或 replace 改窄目标后再继续。停滞计数持久化在 `GoalRecord.checkpointStalls`（缺省即 0），守护进程重启或会话恢复不能清零；找到余量的检查点清零，瞬时校验失败与空回合保留计数；开启新证据窗口的控制动作（edit、replace 与恢复因证据受限停止的目标）清零，从 paused、blocked 恢复保留计数。不新增 limitKind，wire 协议无变化；提交未修改用户文档，官方用户文档尚未同步，合入 main 尚未发布。Claude Code v2.1.246（2026-08-25 发布）官方更新日志为 `/goal` 记录：空闲会话就长时后台工作每个目标至多发起 3 次检查（check-in），下一条用户消息再允许 3 次。Codex、Kimi Code、Qoder CLI 的 `/goal` 记录本次核对无变化。

## 修正

- `cmd-goal`（目标管理）矩阵 Qwen Code 列由 "`/goal`" 更新为 "`/goal` · 条件：main 分支（尚未发布）起证据检查点连续 3 次停滞自动停止目标"。Claude Code、Codex、Kimi Code、Qoder CLI 矩阵结论不变（Claude Code v2.1.246 检查上限记入详情，与此前 v2.1.234、v2.1.239 行为的记录方式一致）。
- `cmd-goal` 详情：Qwen Code 条件补入证据检查点停滞熔断的判定（窗口截断 + 满额 32 条 claims，或窗口截断时校验器输出无法折叠成 claims）、`usage_limited` 落定与固定原因、`GoalRecord.checkpointStalls` 持久化与清零/保留规则，来源新增固定到合并提交 SHA 的停滞熔断提交、停滞常量与原因源码、检查点停滞判定源码；Claude Code 条件补入 v2.1.246 起空闲会话每个目标至多 3 次检查、下一条用户消息再允许 3 次，来源新增固定到提交 SHA 的 v2.1.246 更新日志；跨产品事实新增两条对应条目。
- `docs/01-Slash命令矩阵.md`：目标行 Qwen Code 列按上述结论更新。
- `docs/09-版本与证据.md`：Claude Code 核对日期由 2026-08-25 更新为 2026-08-26，`/goal` 材料补充 v2.1.246 空闲检查上限；Qwen Code 核对日期由 2026-08-25 更新为 2026-08-26，主要材料新增 Goal 证据检查点停滞熔断条目；官方来源表 Claude Code 命令列新增 v2.1.246 更新日志链接，Qwen Code 命令列新增停滞熔断提交、停滞常量与原因源码、检查点停滞判定源码链接。
- `site/data.js` 新增来源 `claude-goal-v246`（v2.1.246 更新日志，固定提交 SHA `005c5dade90c`）、`qwen-goal-stall-commit`（合并提交 `463809cbb39315b11dd9e410a2bcf5d52107418c`）、`qwen-goal-stall-protocol`（同一提交的 `goal-protocol.ts`）、`qwen-goal-stall-checkpoint`（同一提交的 `goal-checkpoint.ts`）；`updatedAt` 更新为 2026-08-26。
- 能力字段总数不变（112 个）。`npm run generate` 重新生成 `docs/capabilities/commands/cmd-goal.md`；`npm test` 通过。

## 影响页面

- [Slash 命令矩阵](../docs/01-Slash命令矩阵.md)
- [目标管理详情](../docs/capabilities/commands/cmd-goal.md)
- [版本与证据](../docs/09-版本与证据.md)

## 证据版本

- Qwen Code PR #9975 "feat(goal): stop a Goal whose checkpoints stalled three times in a row"，2026-08-25T14:08:50Z 合入 main，合并提交 `463809cbb39315b11dd9e410a2bcf5d52107418c`，不属于任何已发布 Release。PR 说明：#9835 让截断的证据窗口压缩而不是停止 Goal，移除了压缩无缓解路径上唯一的终止器；检查点在“压缩的窗口已截断且带回满额 claims”时记为停滞，压缩的两个杠杆（把证据折叠成 claims、把游标移过已折叠内容）此时一个已耗尽（下次检查点只能合并）、一个跟不上（留下了合格证据）；忙碌回合截断但 claims 仍有余量不算停滞，安静 Goal 的满额 claims 列表也不算；停滞串以 `GoalRecord.checkpointStalls` 持久化（缺省为 0）以免重启或恢复清零；任何找到余量的检查以及 edit、replace 重置；连续 3 次停滞后 Goal 以既有的 `limitKind: 'evidence_catalog'` 落定为 `usage_limited`，原因写明停滞内容、次数与处理办法；"No new limit kind, no branch in `goalLimitKindForReason`, nothing crosses the wire."。
- 同一提交 `packages/core/src/goals/goal-protocol.ts`：`export const GOAL_CHECKPOINT_STALL_LIMIT = 3;`（JSDoc："How many consecutive stalled checkpoints a Goal may run before it stops. Three matches the thrash bounds elsewhere in this family of runtimes: one stalled checkpoint is a busy turn, two is a pattern, three is the loop."）；`export const GOAL_CHECKPOINT_CLAIM_LIMIT = 32;`；`GOAL_CHECKPOINT_STALLED_REASON` 原文："The current Goal revision ran three consecutive evidence checkpoints without relief: the evidence window overflowed every time, and each check either came back with a full claim list or a result that could not be folded into claims at all, so every turn paid a checkpoint call and lost uncatalogued evidence. Automatic retries cannot recover. Edit or replace the Goal with a narrower objective before resuming it."；`GoalRecord.checkpointStalls?: number` 字段 JSDoc 说明持久化在记录而非内存、重启或恢复不能清零、找到余量的检查与 edit、replace、恢复证据受限 Goal 的 resume 重置。`GoalStatus` 含 `usage_limited`，`GoalLimitKind` 含 `evidence_catalog`。
- 同一提交 `packages/core/src/goals/goal-checkpoint.ts`：`isGoalCheckpointStalled(window, checkpoint)` 判定为 `window.truncated && checkpoint.claims.length >= GOAL_CHECKPOINT_CLAIM_LIMIT`；`InvalidGoalCheckpointError` 覆盖校验器结果结构非法、claims 为空或超上限、claim 畸形、长度非法、来源引用非法、字节数超限等无法折叠成 claims 的输出。
- 同一提交 `packages/core/src/goals/goal-runtime.ts`：`finishCheckpointCheck` 仅在 `outcome === 'room'` 时把 `checkpointStalls` 清零，`outcome === 'stalled'` 时加 1，其余（瞬时失败、空回合等）保留原值（注释："Only a check that found room ends a stall streak … An unusable verifier result while the window overflowed counts like a stalled checkpoint."）；窗口截断时的 `InvalidGoalCheckpointError` 走 `stalled`，窗口尚有余量时的不可用结果走仅记账路径（注释："A transient failure, or an unusable result while the window still has room, must not abort a healthy Goal"）；空回合（`current_turn_not_tail`）同样仅记账。`settleIfCheckpointStalled` 在 `checkpointStalls >= GOAL_CHECKPOINT_STALL_LIMIT` 时调用 `settleCheckpointFailure`，落定快照为 `status: 'usage_limited'`、`lastReason: GOAL_CHECKPOINT_STALLED_REASON`、`limitKind: 'evidence_catalog'`、`activity: 'idle'` 并 `broadcast('usage_limited')`。
- 同一提交 `packages/core/src/goals/goal-reducer.ts`：`edit` 把 `checkpointStalls` 置为 `undefined` 并重置 `limitKind`/`lastReason`；`replace` 经 `createGoal` 新建不含 `checkpointStalls` 的记录；`usage_limited` 且证据受限的 Goal 恢复时把 `checkpointStalls` 置为 `undefined`（注释："The streak counts checkpoints against one window; this resume starts a different one, so carrying it over would spend the new window's allowance on the old window's failures."），其余恢复不改计数；解析持久化记录时透传 `checkpointStalls`。
- Claude Code 官方更新日志 v2.1.246（提交 `005c5dade90c`，2026-08-25T22:31:44Z；Release v2.1.246，2026-08-25T22:31:52Z）原文："`/goal`: Changed idle sessions to start at most three check-ins on long-running background work per goal; your next message allows three more"。v2.1.245 更新日志仅修复 glibc 2.44 发行版启动崩溃；CHANGELOG.md 无 2.1.244 小节。
- Codex、Kimi Code、Qoder CLI 的 `/goal` 记录本次未变化：快照内 Codex main 分支近期提交（transcript overlay、MCP 断言、Guardian 审查会话、MCP OAuth IdP 解析等）、Kimi Code 近期提交（kap-server 后台上报、rc 输出换行、infinite retry 等）与 Qoder CLI 官方文档均未涉及 `/goal` 行为。Kimi Code `KIMI_CODE_INFINITE_RETRY`（PR #3240，提交 `e6a302b310a7`）为重试模式新能力，不属于本字段，留待后续研究单元核对。
