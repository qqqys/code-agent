# Subagent 能力独立详情页

## 本次更新

- 为 Subagent 域的 22 个能力增加独立网页详情。
- 每个详情页固定记录定义、比较边界、跨产品事实和五家逐项记录。
- 逐产品记录覆盖矩阵结论、入口与配置、定义格式、具体行为、作用域、上下文与继承、工作区隔离、运行限制、条件和证据。
- 增加 22 份对应 Markdown，网页与文档使用同一份结构化数据生成。
- 详情页会根据能力域展示对应字段，不再把 Subagent 内容套入 Slash 命令字段。

## 同步修订

- Qwen Code 的内置 Agent 更新为 `general-purpose` 与 `Explore`。
- Claude Code 的 Agent 字段补充 `disallowedTools`、`effort`、`mcpServers`、`maxTurns`、`hooks`、`memory` 和 `isolation: worktree`。
- Qwen Code 的 Agent 字段补充 `maxTurns`、`mcpServers`、`hooks` 和 Agent 调用级 Worktree 隔离，并保留尚未落地字段的边界。
- Qoder CLI 的详情补充 `maxTurns`、`timeoutMins`、`effort`、`memory`、`hooks` 和 Worktree 隔离。

## 检查范围

- 22 个 Subagent 矩阵字段均有详情数据和 Markdown 页面。
- 每个详情都包含五家产品记录。
- 已完成详情总数为 50 个，共 250 条逐产品记录。
